server:
  port: 10010

spring:
  application:
    name: clientdemoa
  profiles:
    active: dev
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: configserver